# 态势感知系统 PRD — 模块级需求对照文档

本文档按原型中的功能模块组织，每个模块包含功能名称、用户场景、业务规则、输入输出、异常处理、验收标准，并给出原型元素映射，便于设计与开发人员直接对照使用。


## 模块一：数据转换提取配置


### 功能名称

数据报表导出模板管理、可视化图表导出模板管理、导出任务执行与辅助功能。


### 用户场景

数据治理专员或业务人员需要按照政务数据报送、业务分析、大屏展示等要求，将单位、部门、用户及业务数据按固定格式导出为报表或可视化图表；在大数据量、敏感数据、定时/异步场景下，需要分页、脱敏、进度、通知和历史记录支撑。


### 业务规则

- 模板名称在同一范围内唯一，必填字段校验通过后方可保存，保存后默认启用。
- 导出字段选择、数据脱敏规则、分页参数、导出格式均属于"模板配置项"。
- 预览数据量限制在合理范围，敏感数据默认脱敏展示。
- 大文件导出采用异步任务，任务完成后保留导出历史，支持查询、下载、重新导出。
- 所有导出操作记录操作日志。
- 修改模板生成版本记录，支持查看历史版本和修改记录详情（修改人、修改时间、修改内容对比）。
- 支持模板复制，基于已有模板快速创建相似模板。
- 数据报表导出支持字段配置：用户可选择需要导出的字段，调整字段顺序。
- 数据报表导出支持过滤条件配置：可设置导出数据的筛选条件，仅导出符合条件的数据，支持多条件组合。
- 支持多种导出格式：数据报表支持 Excel、PDF、CSV；可视化图表支持 PNG、JPG、PDF、SVG。
- 支持大数据量分页导出，避免文件过大，可按每页行数拆分多个文件。
- 支持导出预览：导出前可预览数据内容，确保数据准确性。
- 导出进度实时显示：展示当前导出进度百分比、已处理条数、预计剩余时间。
- 导出完成后支持系统通知：通过站内信或消息中心通知用户导出完成，提供下载入口。
- 支持导出历史记录：记录用户的导出操作历史，包括导出时间、模板名称、状态、文件大小，支持快速重新导出。
- 支持数据脱敏选择：导出时可对敏感字段（如手机号、身份证号）进行脱敏处理，可配置脱敏规则。
- 可视化图表导出支持数据表配置：可选择与其他模块数据进行组合展示，支持多表关联配置。
- 可视化图表导出支持多种图表类型：折线图、柱状图、饼图、雷达图、面积图、仪表盘、散点图等。
- 可视化图表导出支持图表组合：可将多个图表组合到一个导出文件中，支持自由布局和排列。
- 可视化图表导出支持图表交互配置：可配置是否导出交互功能（如 tooltip、数据钻取、图表筛选联动）。
- 可视化图表导出支持数据源配置：支持从多种数据源获取数据，包括数据库、API 接口、文件导入等。
- 可视化图表导出支持样式与布局配置：可自定义图表颜色主题、字体、图例位置、标题样式、边距、背景等。
- 可视化图表导出支持分辨率配置：可设置导出图片的分辨率（72dpi / 150dpi / 300dpi），满足不同场景需求。


### 输入

- 用户权限身份信息。
- 模板基础信息（名称、描述、格式、类型）。
- 字段配置：字段选择、字段顺序调整。
- 过滤条件配置：筛选字段、运算符、筛选值，支持多条件组合。
- 排序规则：排序字段、排序方式。
- 导出格式选择：报表支持 Excel / PDF / CSV；图表支持 PNG / JPG / PDF / SVG。
- 分页导出配置：是否分页、每页数据量。
- 数据脱敏配置：是否启用脱敏、脱敏字段选择、脱敏规则（掩码/替换/加密）。
- 导出方式（立即/定时）、定时规则（cron 表达式或可视化选择）。
- 可视化图表专属配置：
  - 数据表配置：数据源表选择、关联表配置、关联字段
  - 图表类型选择：折线图/柱状图/饼图/雷达图/面积图/仪表盘/散点图等
  - 图表组合：多图表组合方式、布局排列、间距设置
  - 图表交互：tooltip、数据钻取、筛选联动开关
  - 数据源配置：数据源类型、连接信息、数据查询语句
  - 样式与布局配置：颜色主题、字体、图例位置、标题样式、边距、背景
  - 分辨率配置：72dpi / 150dpi / 300dpi
- 导出任务触发指令。


### 输出

- 报表/图表导出模板列表（含模板名称、类型、状态、创建人、更新时间、版本号）。
- 导出任务信息（任务 ID、进度百分比、已处理条数、状态、预计剩余时间）。
- 导出的文件（Excel / CSV / 图片 / PDF / SVG）。
- 站内信/系统通知（导出完成通知，含下载链接）。
- 操作日志记录。
- 版本历史记录（版本号、修改人、修改时间、修改内容）。
- 修改记录详情（字段变更前后对比）。
- 导出预览数据（导出前预览的数据集）。
- 导出历史记录列表（导出时间、模板名称、操作人、状态、文件大小、下载链接、重新导出入口）。


### 异常处理

- 无权限：提示"暂无权限"并禁用操作入口。
- 数据校验失败：高亮错误字段并提示具体原因。
- 导出任务失败：记录失败原因，通知提交人，支持重新导出。
- 系统异常：记录日志并提示"操作失败，请联系管理员"。
- 模板名称重复：提示"模板名称已存在，请更换"。
- 导出文件过大：自动启用分页导出，提示用户文件将拆分为多个。
- 数据源连接失败：提示数据源连接异常，请检查配置。
- 公式/语法校验失败（图表配置）：高亮错误位置并提示具体错误信息。


### 验收标准

- 支持导出模板的创建、编辑、复制、删除、批量删除、版本管理。
- 模板配置包含导出字段选择、字段顺序调整、数据脱敏、分页设置、导出格式。
- 支持过滤条件配置，多条件组合筛选导出数据。
- 导出流程包含预览、进度展示（百分比+预计时间）、系统通知。
- 导出历史记录独立管理，可查询、下载、重新导出。
- 敏感数据在预览和导出文件中均脱敏，支持多种脱敏规则。
- 可视化图表支持多类型选择、多图表组合、交互功能配置。
- 可视化图表支持多数据源配置、样式与布局自定义、分辨率设置。
- 修改记录可查看，支持字段变更前后对比。
- 支持定时导出和手动立即导出。
- 所有导出操作均记录操作日志。


### 原型元素映射


| 原型元素 | 需求条目 |
| --- | --- |
| 左侧菜单"数据转换提取配置" | 模块入口 |
| "数据报表导出"/"可视化图表导出"Tab | 两类导出模板管理 |
| "新增/编辑模板"弹窗 | 模板基础信息、字段选择、脱敏、分页、过滤条件、排序 |
| 模板列表"导出"按钮 | 导出预览 → 进度 → 通知流程 |
| "版本历史"面板 | 版本管理与修改记录查看 |
| "导出历史"列表 | 历史导出记录与重新导出 |
| 图表配置区域 | 图表类型、数据源、样式布局、分辨率、交互配置 |
| 模板复制功能 | 基于现有模板快速创建 |


## 模块二：数据查询


### 功能名称

索引管理与查询（单位部门用户索引、待办事项索引、群组索引、网络电话索引、活跃度索引、日程索引、应用索引、行为索引），支持新增、编辑、删除、批量删除、多条件查询、排序、预警、导出、备份恢复等全生命周期管理。


### 用户场景

数据治理人员需要对各类业务索引进行管理和维护，包括创建索引、配置索引属性、查询索引数据；最终用户通过索引快速检索业务数据；系统需保证索引健康运行，异常时及时预警。


### 业务规则

- 索引编码/名称在同类型内唯一，不可重复。
- 索引支持启用/禁用状态，禁用后不再参与搜索。
- 查询结果支持排序：按相关性、更新时间、点击量等单字段排序，并支持按多个字段组合排序。
  - **点击量**：用户在搜索结果中点击该索引条目的累计次数，由系统自动统计累加。新建索引时初始点击量为 0，支持在新增/编辑时设置初始值（用于数据迁移或初始化场景）。
  - **相关性**：系统根据用户输入的关键词与索引各字段（名称、编码、标签等）的匹配程度自动计算的匹配得分。匹配规则遵循"完全匹配 > 前缀匹配 > 包含匹配"的层级，字段权重采用系统内置默认策略（名称权重最高、编码次之、标签最低），无需用户手动配置。
- 索引预警：当索引出现异常（同步失败、延迟超限、数据校验异常、索引损坏等）时，系统自动发出告警，通知管理员处理。预警支持不同级别（一般/严重/紧急）。
- 支持模糊查询：支持在名称、编码等字段上进行模糊匹配搜索。
- 支持关键字查询：通过关键词快速筛选数据，跨字段匹配。
- 支持多条件查询：支持组合多个查询条件进行复杂搜索，满足高级筛选需求。
- 支持查询自动补全：用户输入关键词时，系统实时提供搜索建议或自动补全候选。
- 支持按状态查询：可按启用/禁用/异常等状态筛选索引。
- 支持按标签查询：可按索引标签进行分类筛选，支持标签筛选条件重置。
- 修改索引生成修改记录，支持查看修改记录详情（修改人、修改时间、修改字段）。
- 支持索引刷新：手动触发索引数据刷新，同步最新数据。
- 支持索引备份：提供索引的备份功能，防止数据丢失，支持全量备份和增量备份。
- 支持索引恢复：提供索引的恢复功能，可从备份文件中恢复索引数据。
- 支持自动清理失效索引：可设置索引的过期时间，系统自动删除不再需要的过期索引，支持配置清理策略。
- 支持索引版本管理：记录索引配置的版本历史，支持版本对比和回滚。
- 支持索引导出：可将索引数据导出为文件，支持多种导出格式。
- 索引类型：单位部门用户索引、待办事项索引、群组索引、网络电话索引、活跃度索引、日程索引、应用索引、行为索引，各类型独立管理，功能结构一致。


### 输入

- 用户权限身份信息。
- 索引类型选择（单位/部门/用户/待办/群组/网络电话/活跃度/日程/应用/行为）。
- 索引基础信息：名称、编码、描述、标签、状态（启用/禁用）。
- 初始点击量（新增时设置，用于数据迁移或初始化，默认 0）。
- 过期时间（用于自动清理失效索引）。
- 查询条件：
  - 关键词搜索（模糊查询/关键字查询）
  - 状态筛选（启用/禁用/异常）
  - 标签筛选
  - 多条件组合查询
- 排序条件：排序字段、排序方式（升序/降序）、多字段排序优先级。
- 预警规则配置：预警级别、预警条件、通知方式、通知人。
- 备份/恢复操作指令、备份策略配置。
- 自动清理策略配置：清理周期、过期判定条件。
- 导出格式选择。


### 输出

- 索引列表（含索引名称、编码、状态、标签、点击量、更新时间、相关性得分等）。
- 查询结果集（分页展示，支持排序）。
- 自动补全建议列表。
- 索引详情信息。
- 修改记录列表（修改人、修改时间、修改字段、变更内容）。
- 预警信息（预警级别、预警类型、预警时间、预警描述、处理状态）。
- 备份任务信息（备份ID、状态、进度、备份文件大小）。
- 恢复任务信息（恢复ID、状态、进度）。
- 导出文件。
- 版本历史记录（版本号、创建时间、操作人、版本说明）。
- 操作日志记录。


### 异常处理

- 索引名称/编码重复：提示"索引名称/编码已存在，请更换"。
- 数据校验失败：高亮错误字段并提示具体原因。
- 索引同步异常：触发索引预警，记录异常日志，通知管理员。
- 备份/恢复失败：记录失败原因，提示用户重试。
- 无权限：禁用对应操作按钮，提示"暂无权限"。
- 系统异常：记录日志并提示"操作失败，请联系管理员"。
- 索引损坏：自动触发预警并尝试修复，修复失败则通知管理员。


### 验收标准

- 支持 10 类索引的新增、编辑、删除、批量删除、查询。
- 支持模糊查询、关键字查询、多条件组合查询。
- 支持查询自动补全，输入关键词时实时给出建议。
- 查询结果支持排序：按相关性、时间、点击量单字段排序，以及多字段组合排序。
- 支持按状态筛选、按标签筛选，支持标签筛选条件重置。
- 支持索引预警：索引异常时自动触发告警，支持不同预警级别。
- 支持查看修改记录详情。
- 支持索引刷新：手动触发数据刷新。
- 支持索引备份与恢复：全量备份、增量备份、从备份恢复。
- 支持自动清理失效索引：可配置过期时间和清理策略。
- 支持索引版本管理与版本回滚。
- 支持索引数据导出。
- 支持索引统计：可统计各类型索引的总量、启用数、异常数、点击量排行等指标，支持按时间维度统计索引使用情况。
- 相关性排序由系统内置算法自动计算，无需用户配置。
- 点击量由系统自动统计累加，新建时支持设置初始值。


### 原型元素映射


| 原型元素 | 需求条目 |
| --- | --- |
| 左侧菜单"数据查询" | 模块入口 |
| 索引类型 Tab 切换 | 10 类索引分类管理 |
| 索引列表 | 查询展示、排序、状态标签 |
| 搜索框 + 自动补全 | 关键词查询、模糊查询、自动补全 |
| 高级筛选面板 | 多条件查询、状态筛选、标签筛选 |
| "新增/编辑索引"弹窗 | 索引基础信息、初始点击量、状态、标签、过期时间 |
| 列表表头排序按钮 | 单字段排序、多字段排序配置 |
| 预警横幅 + 预警详情弹窗 | 索引异常预警展示与处理 |
| "修改记录"面板 | 修改记录查看 |
| "备份/恢复"操作 | 索引备份与恢复 |
| "导出"按钮 | 索引数据导出 |
| "刷新"按钮 | 索引数据刷新 |
| 版本管理面板 | 版本历史、版本回滚 |


## 模块三：数据过滤


### 功能名称

多维度数据过滤（单位基本信息、部门基本信息、行政区划信息、业务条线信息、组织架构信息、时间范围信息、地理位置信息、区域编码信息），支持按条件查询、多条件组合、关键词、分类、标签、层级、时间、状态、范围等多种过滤方式。


### 用户场景

数据分析人员需要从多个维度对基础数据进行筛选和过滤，快速定位目标数据。不同数据类型有不同的过滤维度，如行政区划按层级过滤、组织架构按权限过滤等。


### 业务规则

- 支持 8 类数据的独立过滤：单位基本信息、部门基本信息、行政区划信息、业务条线信息、组织架构信息、时间范围信息、地理位置信息、区域编码信息。
- 支持按条件查询：可针对各字段设置查询条件进行精确筛选。
- 支持多条件组合过滤：可同时设置多个过滤条件，支持"且"/"或"逻辑组合。
- 支持关键词过滤：输入关键词在指定字段范围内进行模糊匹配搜索。
- 支持范围过滤：可对数值型、日期型字段设置范围区间（如人数范围、创建时间范围）。
- 支持按分类过滤：根据数据的分类字段进行筛选（如单位类型、部门类别）。
- 支持按标签过滤：根据数据的标签进行筛选。
- 支持多级分类过滤：支持多层级分类的级联筛选（如行业大类 → 子类 → 细类）。
- 支持按时间范围过滤：支持按固定时间范围（今天、本周、本月、本季度、本年）筛选数据。
- 支持按时间段过滤：支持自定义起止时间的时间段筛选。
- 支持自定义时间过滤：用户可自由选择起止日期时间进行精确过滤。
- 支持按状态过滤：根据数据的状态字段（启用/禁用/已删除等）进行筛选。
- 支持多状态过滤：可同时选择多个状态值进行筛选（如同时筛选"启用"和"禁用"）。
- 支持查询结果按字段排序：可按任意字段进行升序/降序排序。
- 支持过滤历史记录：自动记录用户的过滤条件历史，支持一键快速切换常用过滤方案。
- 支持过滤条件重置：一键清空所有过滤条件，恢复默认状态。
- 支持过滤条件提示：对复杂过滤条件提供操作提示和说明。
- 支持数据刷新：手动刷新当前过滤结果，获取最新数据。
- 支持数据导出：将过滤后的结果数据导出为文件。
- 行政区划信息专属：
  - 区划层级过滤：按省/市/县/乡等行政区划层级进行筛选
  - 模糊搜索过滤：支持行政区划信息按模糊搜索过滤，输入关键词匹配行政区划名称
  - 查询输入自动提示：输入时实时匹配行政区划名称并给出建议
- 业务条线信息专属：
  - 层级过滤：按业务条线的层级结构进行筛选
- 组织架构信息专属：
  - 区划层级过滤：按行政区划层级筛选组织架构
  - 按权限过滤：根据用户权限范围过滤可见的组织架构数据
  - 按层级展示：支持按省/市/县分级展开展示组织架构树
- 部门基本信息专属：
  - 关键字过滤：按关键字筛选部门信息（支持部门基本信息按关键字过滤）
  - 状态过滤：按部门状态进行筛选
- 时间范围信息专属：
  - 按时间范围/时间段/自定义时间过滤
  - 查询结果按字段排序
  - 过滤历史记录、条件重置、条件提示
- 地理位置信息专属：
  - 地理位置信息按地理位置过滤（如按区域、按坐标范围筛选）
  - 按时间段、自定义时间过滤
  - 查询结果排序、过滤历史、条件重置、条件提示
- 区域编码信息专属：
  - 区域编码信息按区域编码过滤
  - 按时间段、自定义时间过滤
  - 查询结果排序、过滤历史、条件重置、条件提示
  - 信息刷新、信息导出


### 输入

- 用户权限身份信息。
- 数据类型选择（单位/部门/行政区划/业务条线/组织架构/时间范围/地理位置/区域编码）。
- 过滤条件：
  - 条件查询：字段、运算符、值
  - 多条件组合：条件列表、逻辑关系（且/或）
  - 关键词：搜索词、搜索字段范围
  - 范围过滤：最小值、最大值
  - 分类过滤：分类值（支持多级分类级联）
  - 标签过滤：标签列表
  - 时间范围过滤：快捷选项（今天/本周/本月/本季度/本年）
  - 时间段过滤：开始时间、结束时间
  - 自定义时间过滤：精确起止时间
  - 状态过滤：状态值（支持多选）
  - 区划层级过滤：层级选择（省/市/县/乡）
  - 层级过滤：业务条线层级
  - 按权限过滤：权限范围
  - 按地理位置过滤：区域/坐标范围
  - 按区域编码过滤：编码值/编码范围
- 排序条件：排序字段、排序方式。
- 分页参数：页码、每页条数。


### 输出

- 过滤后的结果列表（分页展示）。
- 过滤条件摘要（当前生效的过滤条件汇总展示）。
- 过滤历史记录列表（过滤方案名称、条件摘要、使用时间）。
- 排序后的结果集。
- 自动补全建议列表（行政区划/关键字输入时）。
- 组织架构树（按层级展开展示）。
- 导出文件（过滤后的数据）。
- 操作日志记录。


### 异常处理

- 无权限：提示"暂无权限"并限制可见数据范围。
- 过滤条件格式错误：高亮错误条件并提示正确格式。
- 无匹配数据：展示空状态并提示"暂无符合条件的数据"。
- 数据量过大：自动启用分页加载，支持滚动加载或分页切换。
- 系统异常：记录日志并提示"操作失败，请联系管理员"。
- 层级数据加载失败：提示"数据加载失败，请刷新重试"。


### 验收标准

- 支持 8 类数据的过滤查询。
- 支持按条件查询、多条件组合过滤（且/或逻辑）。
- 支持关键词过滤、模糊搜索、查询输入自动提示。
- 支持范围过滤、分类过滤、标签过滤、多级分类过滤。
- 支持按时间范围、时间段、自定义时间过滤。
- 支持按状态过滤、多状态过滤。
- 支持查询结果按字段排序（升序/降序）。
- 支持过滤历史记录：自动记录，可快速切换。
- 支持过滤条件一键重置。
- 支持过滤条件操作提示。
- 支持数据手动刷新。
- 支持过滤结果数据导出。
- 行政区划信息支持区划层级过滤、模糊搜索、自动提示。
- 业务条线信息支持层级过滤。
- 组织架构信息支持区划层级过滤、按权限过滤、按层级展示（树状结构）。
- 部门基本信息支持关键字过滤、状态过滤。
- 地理位置信息支持按地理位置过滤。
- 区域编码信息支持按区域编码过滤。


### 原型元素映射


| 原型元素 | 需求条目 |
| --- | --- |
| 左侧菜单"数据过滤" | 模块入口 |
| 数据类型 Tab 切换 | 8 类数据分类过滤 |
| 筛选条件面板 | 多条件组合、分类、标签、状态、时间范围等过滤 |
| 搜索框 + 自动补全 | 关键词过滤、模糊搜索、自动提示 |
| 范围筛选控件 | 数值范围、时间范围过滤 |
| 级联选择器 | 多级分类过滤、区划层级过滤 |
| 列表表头排序 | 按字段排序 |
| 过滤历史下拉 | 过滤历史记录快速切换 |
| "重置"按钮 | 过滤条件重置 |
| "刷新"按钮 | 数据刷新 |
| "导出"按钮 | 过滤结果导出 |
| 组织架构树 | 按层级展示、按权限过滤 |
| 条件提示图标/文案 | 过滤条件操作提示 |


## 模块四：数据模型管理


### 功能名称

统计模型管理（用户统计模型、待办统计模型、应用统计模型、行为统计模型），支持模型新增、编辑、删除、批量删除、查询、复制、版本管理、备份恢复、导出、修改记录查看等全生命周期管理。
### 用户场景

数据分析人员需要定义"用户活跃度""部门协同度"等统计指标模型，为态势感知图表和大屏提供计算依据。模型需支持版本管理、备份恢复，确保数据计算的稳定性和可追溯性。


### 业务规则

- 模型编码唯一，不可重复。
- 支持 4 类统计模型：用户统计模型、待办统计模型、应用统计模型、行为统计模型，各类型独立管理。
- 统计维度支持日、周、月多选，可自由组合。
- 指标公式可配置，保存前需语法校验，支持常用统计函数（求和、平均、计数、最大、最小等）。
- 模型支持启用/禁用，禁用后不再参与计算。
- 修改模型生成版本记录，支持查看历史版本和修改记录详情（修改人、修改时间、修改字段对比）。
- 支持模型复制：基于现有模型快速创建相似模型，复制后自动生成新编码。
- 支持模型版本管理：记录模型配置的版本历史，支持版本对比和回滚到指定版本。
- 支持模型备份：提供模型配置和数据的备份功能，防止数据丢失，支持全量备份。
- 支持模型恢复：从备份文件中恢复模型配置和数据，恢复前需确认覆盖。
- 支持模型刷新：手动触发模型重新计算，刷新统计结果。
- 支持模型导出：可将模型配置导出为文件，支持在不同环境间迁移。
- 支持模型批量删除：可选中多个模型一次性删除，删除前需二次确认。
- 模型计算结果与模型配置关联展示，便于追溯数据来源。
- 待办统计模型、应用统计模型、行为统计模型支持查看修改记录。


### 输入

- 用户权限身份信息。
- 模型类型选择（用户统计/待办统计/应用统计/行为统计）。
- 模型基础信息：编码、名称、描述、状态（启用/禁用）。
- 统计维度（日/周/月复选，支持多选组合）。
- 指标公式配置：指标名称、计算公式、数据来源字段、统计函数。
- 过滤条件：模型计算的数据范围筛选条件。
- 复制操作指令（选择源模型）。
- 版本操作指令（版本对比、版本回滚）。
- 备份操作指令（备份类型、备份说明）。
- 恢复操作指令（选择备份文件、确认覆盖）。
- 刷新操作指令（手动触发重算）。
- 导出格式选择。
- 批量删除：选中的模型 ID 列表。


### 输出

- 模型列表（按类型分类，含模型名称、编码、状态、统计维度、更新时间、版本号）。
- 模型配置详情。
- 模型计算结果（按统计维度聚合的指标数据）。
- 版本历史记录（版本号、修改人、修改时间、版本说明）。
- 修改记录详情（字段变更前后对比）。
- 复制生成的新模型信息。
- 备份任务信息（备份ID、状态、文件大小、完成时间）。
- 恢复任务信息（恢复ID、状态、进度）。
- 导出文件（模型配置）。
- 操作日志记录。


### 异常处理

- 编码重复：提示"模型编码已存在"。
- 公式校验失败：高亮公式输入框并提示具体错误信息。
- 无权限：禁用保存/删除/备份等操作按钮。
- 模型计算失败：记录失败原因，标记模型状态为异常，通知管理员。
- 备份/恢复失败：记录失败原因，提示用户重试。
- 批量删除部分失败：提示成功和失败数量，列出失败原因。
- 版本回滚失败：提示回滚失败原因，当前版本不受影响。
- 系统异常：记录日志并提示"操作失败，请联系管理员"。


### 验收标准

- 支持 4 类统计模型的新增、编辑、删除、批量删除、查询。
- 统计维度支持多选（日/周/月）。
- 模型状态可启用/禁用，禁用后停止计算。
- 指标公式可配置，保存前语法校验。
- 支持模型复制：基于现有模型快速创建。
- 版本历史可查，支持版本对比和版本回滚。
- 支持查看修改记录详情（字段变更对比）。
- 支持模型备份与恢复：全量备份、从备份恢复。
- 支持模型手动刷新（重新计算）。
- 支持模型配置导出。
- 模型计算结果与配置关联展示。


### 原型元素映射


| 原型元素 | 需求条目 |
| --- | --- |
| 左侧菜单"数据模型管理" | 模块入口 |
| 模型类型 Tab 切换 | 4 类统计模型分类管理 |
| 模型列表 | 模型查询与展示、批量选择 |
| "新增/编辑统计模型"弹窗 | 模型信息、统计维度、指标公式、过滤条件 |
| 统计维度复选框 | 日/周/月维度选择 |
| 状态开关/下拉 | 启用/禁用控制 |
| 公式编辑器 | 指标公式配置与语法校验 |
| "复制"按钮 | 模型复制功能 |
| 版本历史面板 | 版本管理、版本对比、版本回滚 |
| "修改记录"面板 | 修改记录详情查看 |
| "备份/恢复"按钮 | 模型备份与恢复 |
| "刷新"按钮 | 模型手动刷新重算 |
| "导出"按钮 | 模型配置导出 |
| 批量删除操作 | 批量删除模型 |


## 模块五：数据图表管理


### 功能名称

可视化图表管理（用户统计可视化、待办统计可视化、应用统计可视化、行为统计可视化），支持数据来源配置、指标配置、刷新频率、样式配置、布局配置、数据推送、链接公开、权限配置等功能。


### 用户场景

数据可视化工程师需要配置各类统计数据的可视化展示，包括图表样式、布局、数据源等，为态势感知大屏和报表提供可视化组件。需要支持灵活的样式配置和多权限访问控制。


### 业务规则

- 支持 4 类可视化图表：用户统计可视化、待办统计可视化、应用统计可视化、行为统计可视化，各类型独立配置管理。
- 支持数据来源配置：配置用户统计可视化信息数据来源、待办统计可视化信息数据来源、应用统计可视化信息数据来源、行为统计可视化信息数据来源，支持从数据库、API 接口、文件等多种数据源获取数据。
- 支持数据指标配置：配置需要展示的统计指标（如用户数量、新增用户、活跃用户、留存用户、待办数量、新增待办、应用数量、行为数量等）。
- 支持刷新频率配置：配置数据刷新的频率（如实时、每分钟、每小时、每天等），可设置自动刷新策略。
- 支持样式配置：配置图表的样式，包括颜色主题、字体、图例位置、标题、坐标轴、标签、tooltip 样式等。
- 支持布局配置：配置可视化大屏/看板中图表的位置、大小和排列方式，支持拖拽式布局调整。
- 支持数据推送配置：配置数据推送机制（如 WebSocket、轮询），支持实时数据更新推送。
- 支持链接公开：生成可视化图表或大屏的公开访问链接，支持外部用户无需登录访问。
  - 公开链接支持设置有效期、访问密码、访问次数限制。
  - 支持公开链接的启用/禁用和撤销。
- 支持权限配置：配置可视化图表的访问权限和编辑权限，控制哪些用户/角色可以查看或编辑。
  - 支持按角色、按部门、按用户三种维度配置权限。
  - 支持查看权限和编辑权限的细分控制。
- 支持手动刷新：一键刷新当前图表数据，获取最新计算结果。
- 图表配置保存后生成版本记录，支持查看历史配置。
- 支持图表预览：配置过程中可实时预览图表效果。


### 输入

- 用户权限身份信息。
- 可视化类型选择（用户统计/待办统计/应用统计/行为统计）。
- 图表基础信息：名称、描述、类型（折线图/柱状图/饼图/雷达图/面积图/仪表盘等）。
- 数据来源配置：
  - 数据源类型（数据库/API/文件）
  - 连接信息或接口地址
  - 数据查询语句或参数
- 数据指标配置：
  - 指标选择（如用户数、新增用户、活跃用户、留存用户等）
  - 指标展示名称
  - 指标单位
  - 指标颜色
- 刷新频率配置：刷新方式（定时/实时/手动）、刷新间隔（秒/分/时/天）。
- 样式配置：
  - 颜色主题（预设主题/自定义）
  - 字体（字体族、字号、颜色）
  - 图例（位置、显示/隐藏、样式）
  - 标题（内容、位置、样式）
  - 坐标轴（显示/隐藏、刻度、标签）
  - tooltip（显示/隐藏、内容格式）
- 布局配置：
  - 图表位置（x、y 坐标或网格位置）
  - 图表大小（宽度、高度）
  - 排列方式（自由布局/网格布局）
- 数据推送配置：
  - 推送方式（WebSocket/轮询/SSE）
  - 推送频率
  - 推送条件（数据变更时/定时）
- 链接公开配置：
  - 是否公开
  - 公开链接有效期
  - 访问密码（可选）
  - 访问次数限制（可选）
- 权限配置：
  - 查看权限（角色/部门/用户）
  - 编辑权限（角色/部门/用户）
- 预览操作指令、刷新操作指令。


### 输出

- 可视化图表列表（按类型分类，含图表名称、类型、状态、数据源、更新时间）。
- 图表配置详情。
- 图表渲染结果（可视化展示）。
- 实时推送的数据更新。
- 公开链接信息（链接地址、有效期、访问密码、访问统计）。
- 权限配置结果（有权限的用户/角色/部门列表）。
- 预览效果（配置过程中实时预览）。
- 刷新后的最新数据。
- 操作日志记录。


### 异常处理

- 数据源连接失败：提示"数据源连接异常，请检查配置"，图表显示占位状态。
- 数据查询失败：提示"数据加载失败"，展示错误原因。
- 无权限：提示"暂无权限查看"，隐藏或禁用对应图表。
- 公开链接失效：提示"链接已过期或已撤销"。
- 样式配置错误：高亮错误配置项并提示修正建议。
- 推送连接断开：自动重连，重连失败提示用户并切换为手动刷新。
- 系统异常：记录日志并提示"操作失败，请联系管理员"。
### 验收标准
- 支持 4 类可视化图表的配置管理。
- 支持多数据源配置（数据库/API/文件）。
- 支持数据指标配置：可选择指标、设置展示名称和单位。
- 支持刷新频率配置：定时/实时/手动多种刷新策略。
- 支持样式配置：颜色主题、字体、图例、标题、坐标轴、tooltip 等。
- 支持布局配置：图表位置、大小、排列方式可调整。
- 支持数据推送配置：WebSocket/轮询/SSE 等推送方式。
- 支持链接公开：可生成公开访问链接，支持有效期、密码、访问次数限制。
- 支持权限配置：按角色/部门/用户配置查看和编辑权限。
- 支持手动刷新图表数据。
- 支持配置过程中实时预览图表效果。
- 支持图表配置版本记录。


### 原型元素映射


| 原型元素 | 需求条目 |
| --- | --- |
| 左侧菜单"数据图表管理" | 模块入口 |
| 可视化类型 Tab 切换 | 4 类可视化分类管理 |
| 图表列表 | 图表查询与展示 |
| 图表配置面板 | 数据源、指标、样式、布局、推送、权限配置 |
| 数据源配置区 | 数据源类型、连接信息、查询配置 |
| 指标配置区 | 指标选择、展示名称、单位、颜色 |
| 样式配置区 | 颜色主题、字体、图例、标题、坐标轴、tooltip |
| 布局编辑器 | 拖拽式图表位置和大小调整 |
| 刷新频率设置 | 刷新方式、刷新间隔 |
| 数据推送设置 | 推送方式、频率、条件 |
| 公开链接设置 | 链接生成、有效期、密码、访问限制 |
| 权限配置面板 | 查看权限、编辑权限配置 |
| 图表预览区 | 实时预览图表效果 |
| "刷新"按钮 | 手动刷新数据 |


## 模块六：通知公告管理


### 功能名称

通知公告管理（新增、编辑、删除、发布、撤回、置顶、分类管理、导入）。


### 用户场景

系统管理员需要发布系统通知、公告、更新说明等信息，用户登录后可查看。支持批量导入通知公告，提高维护效率。


### 业务规则

- 通知公告支持分类管理（系统通知、业务公告、更新说明等）。
- 支持草稿、已发布、已撤回三种状态。
- 发布后所有用户可见，撤回后不再展示。
- 支持置顶功能，置顶公告优先展示。
- 支持富文本编辑，可插入图片、链接、表格。
- 支持通知公告导入：可通过文件批量导入通知公告数据。
  - 支持 Excel 格式导入。
  - 导入前支持预览导入数据。
  - 导入时校验数据格式，错误数据提示具体原因。
  - 支持批量导入和逐条导入。
- 所有操作记录操作日志。


### 输入

- 通知公告标题、分类、内容（富文本）。
- 发布状态（草稿/发布）、是否置顶。
- 生效时间、失效时间（可选）。
- 接收范围（全体用户/指定部门/指定角色）。
- 导入文件（Excel 格式）。
- 导入确认指令。


### 输出

- 通知公告列表（含标题、分类、状态、发布时间、置顶标记）。
- 通知公告详情。
- 导入结果（成功数量、失败数量、失败详情）。
- 导入预览数据。
- 操作日志记录。


### 异常处理

- 无权限：禁用发布/删除/导入操作。
- 内容为空：提示"标题和内容不能为空"。
- 导入文件格式错误：提示"请上传正确格式的 Excel 文件"。
- 导入数据校验失败：列出错误行号和错误原因，支持修正后重新导入。
- 系统异常：记录日志并提示"操作失败，请联系管理员"。


### 验收标准

- 支持通知公告的新增、编辑、删除、发布、撤回。
- 支持分类管理和置顶功能。
- 支持富文本编辑，可插入图片、链接、表格。
- 支持通知公告导入（Excel 格式）。
- 导入前支持数据预览。
- 导入时校验数据格式，错误数据有明确提示。
- 支持设置接收范围（全体/指定部门/指定角色）。
- 支持设置生效/失效时间。
- 所有操作记录操作日志。


### 原型元素映射


| 原型元素 | 需求条目 |
| --- | --- |
| 左侧菜单"通知公告管理" | 模块入口 |
| 通知公告列表 | 公告查询与展示、状态管理 |
| "新增/编辑公告"弹窗 | 公告标题、分类、内容、发布设置 |
| 富文本编辑器 | 富文本内容编辑 |
| "发布/撤回"按钮 | 公告发布与撤回 |
| "置顶"操作 | 公告置顶 |
| "导入"按钮 | 通知公告批量导入 |
| 导入预览弹窗 | 导入数据预览与确认 |
| 分类管理面板 | 公告分类维护 |


## 模块七：日志管理


### 功能名称

操作日志与登录日志管理（查询、删除、清空、解锁、导出）。


### 用户场景

系统管理员需要查看和管理系统操作日志与登录日志，用于安全审计和问题排查。支持删除过期日志、清空日志、解锁异常锁定的账号等操作。


### 业务规则

- 操作日志记录所有用户的关键操作，包括操作人、操作时间、操作类型、操作内容、IP 地址、操作结果。
- 登录日志记录所有登录行为，包括登录人、登录时间、登录 IP、登录方式、登录结果、失败原因。
- 日志仅支持查询和删除，不支持修改。
- 支持操作日志删除：可删除指定的操作日志记录。
- 支持操作日志清空：可一次性清空所有操作日志（需二次确认）。
- 支持登录日志删除：可删除指定的登录日志记录。
- 支持登录日志清空：可一次性清空所有登录日志（需二次确认）。
- 支持登录日志解锁：当用户因多次登录失败被锁定时，管理员可手动解锁账号。
  - 解锁需记录操作人和解锁原因。
  - 解锁后账号立即恢复可登录状态。
- 支持按时间范围、操作人、操作类型、登录结果等多条件查询。
- 支持日志导出：可将日志数据导出为文件。
- 日志保留策略可配置，超过保留期的日志自动归档或清理。


### 输入

- 用户权限身份信息（管理员权限）。
- 日志类型选择（操作日志/登录日志）。
- 查询条件：
  - 时间范围（开始时间、结束时间）
  - 操作人/登录人
  - 操作类型/登录方式
  - 操作结果/登录结果（成功/失败）
  - IP 地址
  - 关键词（操作内容/失败原因）
- 删除操作指令（单条删除/批量删除）。
- 清空操作指令（需二次确认）。
- 解锁操作指令（需填写解锁原因）。
- 导出格式选择。


### 输出

- 操作日志列表（操作人、操作时间、操作类型、操作内容、IP、结果）。
- 登录日志列表（登录人、登录时间、登录IP、登录方式、登录结果、失败原因、锁定状态）。
- 日志详情。
- 删除/清空操作结果提示。
- 解锁操作结果提示。
- 导出文件。
- 操作日志记录（管理员操作自身也记录）。


### 异常处理

- 无权限：提示"暂无权限，请联系管理员"。
- 删除/清空操作需二次确认，防止误操作。
- 解锁失败：提示解锁失败原因（如账号未处于锁定状态）。
- 日志数据量过大：自动分页加载，支持按时间范围缩小查询。
- 系统异常：记录日志并提示"操作失败，请联系管理员"。


### 验收标准

- 支持操作日志和登录日志的查询与展示。
- 支持多条件查询：时间范围、操作人、操作类型、结果、IP 等。
- 支持操作日志删除（单条/批量）。
- 支持操作日志清空（需二次确认）。
- 支持登录日志删除（单条/批量）。
- 支持登录日志清空（需二次确认）。
- 支持登录日志解锁：手动解锁锁定账号，记录解锁原因。
- 支持日志数据导出。
- 日志不可修改，仅可查询和删除。
- 所有管理员操作均记录操作日志。
### 原型元素映射


| 原型元素 | 需求条目 |
| --- | --- |
| 左侧菜单"日志管理" | 模块入口 |
| "操作日志"/"登录日志"Tab | 两类日志分类管理 |
| 日志列表 | 日志查询与展示 |
| 筛选条件面板 | 时间范围、操作人、类型、结果、IP 等筛选 |
| "删除"按钮 | 单条/批量删除日志 |
| "清空"按钮 | 清空日志（二次确认） |
| "解锁"按钮 | 解锁锁定账号（填写解锁原因） |
| "导出"按钮 | 日志数据导出 |
| 日志详情弹窗 | 查看日志详细信息 |
| 二次确认弹窗 | 删除/清空操作确认 |


## 模块八：参数管理


### 功能名称

系统参数/字典参数管理（新增、编辑、删除、导入、导出、分类管理）。


### 用户场景

系统管理员需要维护系统的各类参数配置和字典数据，如状态枚举、业务类型、地区编码等。支持批量导入导出，提高参数维护效率。


### 业务规则

- 参数按分类管理，支持多级分类。
- 参数编码在同一分类下唯一，不可重复。
- 参数支持启用/禁用状态，禁用后不再作为可选值。
- 支持参数导入：可通过文件批量导入参数数据。
  - 支持 Excel 格式导入。
  - 导入前支持预览导入数据。
  - 导入时校验编码唯一性和数据格式。
  - 支持新增导入和覆盖导入两种模式。
- 支持参数删除：可删除不再需要的参数信息。
- 支持参数修改：修改已有参数的名称、值、排序、状态等信息。
- 支持参数导出：可导出字典参数列表为文件。
- 支持参数排序：可调整参数的显示顺序。
- 所有操作记录操作日志。


### 输入

- 用户权限身份信息（管理员权限）。
- 参数分类选择/管理。
- 参数基础信息：编码、名称、值、排序、状态（启用/禁用）、描述。
- 导入文件（Excel 格式）。
- 导入模式选择（新增导入/覆盖导入）。
- 删除操作指令（单条/批量）。
- 导出格式选择。


### 输出

- 参数分类树（多级分类展示）。
- 参数列表（按分类筛选，含编码、名称、值、排序、状态）。
- 参数详情。
- 导入结果（成功数量、失败数量、失败详情列表）。
- 导入预览数据。
- 导出文件（字典参数列表）。
- 操作日志记录。


### 异常处理

- 无权限：提示"暂无权限，请联系管理员"。
- 参数编码重复：提示"参数编码已存在，请更换"。
- 导入文件格式错误：提示"请上传正确格式的 Excel 文件"。
- 导入数据校验失败：列出错误行号和错误原因，支持修正后重新导入。
- 删除被引用的参数：提示"该参数已被业务数据引用，无法删除"。
- 系统异常：记录日志并提示"操作失败，请联系管理员"。


### 验收标准

- 支持参数的新增、编辑、删除、查询。
- 支持多级分类管理。
- 支持参数导入（Excel 格式），新增导入和覆盖导入两种模式。
- 导入前支持数据预览，导入时校验数据格式。
- 支持参数删除（单条/批量）。
- 支持参数修改（名称、值、排序、状态）。
- 支持参数导出（导出字典参数列表）。
- 支持参数排序调整。
- 参数支持启用/禁用状态控制。
- 所有操作记录操作日志。


### 原型元素映射


| 原型元素 | 需求条目 |
| --- | --- |
| 左侧菜单"参数管理" | 模块入口 |
| 分类树 | 多级分类导航 |
| 参数列表 | 参数查询与展示 |
| "新增/编辑参数"弹窗 | 参数基础信息配置 |
| "导入"按钮 | 参数批量导入 |
| 导入预览弹窗 | 导入数据预览与模式选择 |
| "导出"按钮 | 参数列表导出 |
| "删除"按钮 | 参数删除 |
| 排序调整控件 | 参数显示顺序调整 |
| 状态开关 | 参数启用/禁用 |


## 模块九：系统对接


### 功能名称

对接"赣政通"平台信创版底座，包括统计信息查询与导出、接口同步频率配置、接口适配管理、接口日志管理、接口日志分类管理、接口权限认证配置等功能。


### 用户场景

系统运维人员需要管理与赣政通平台的对接配置，监控接口运行状态，查看接口日志，配置接口同步频率和权限认证信息，确保对接安全稳定运行。


### 业务规则

- 支持 5 类统计信息的查询与导出：网络电话统计、手机短信强提醒统计、视频会议统计、视频电话统计、聊天窗口统计。
  - 每类统计均支持按时间范围、统计维度等条件查询。
  - 每类统计均支持导出为文件。
- 支持接口对接情况分析：查询接口对接的整体情况、成功率、失败率、调用量趋势等。
  - 支持对接情况分析数据导出。
- 支持接口同步频率配置：配置各接口的数据同步频率。
  - 支持同步频率配置的新增、修改、删除、查询、导出。
  - 可配置同步周期（实时/定时/手动）、同步间隔、同步时间点。
- 支持接口适配信息管理：管理各接口的适配配置信息。
  - 支持接口适配的新增、修改、删除、查询、导出。
  - 可配置接口地址、请求方式、请求参数、响应格式、适配规则等。
- 支持接口日志信息管理：记录所有接口调用日志。
  - 支持接口日志的新增（自动记录）、修改、删除、查询、导出。
  - 记录内容包括：接口名称、调用时间、调用方、请求参数、响应结果、耗时、状态。
- 支持接口日志分类信息管理：对接口日志进行分类管理。
  - 支持分类的新增、修改、删除、查询、导出。
  - 可按业务模块、接口类型、状态等维度分类。
- 支持接口权限认证配置：管理接口调用的权限认证信息。
  - 支持权限认证配置的新增、修改、删除、查询、导出。
  - 支持多种认证方式：API Key、Token、OAuth2.0、数字签名等。
  - 可配置认证有效期、权限范围、调用频率限制等。
- 所有接口操作记录操作日志。
- 接口异常时自动告警，通知运维人员处理。


### 输入

- 用户权限身份信息（运维/管理员权限）。
- 统计查询条件：
  - 统计类型（网络电话/短信强提醒/视频会议/视频电话/聊天窗口）
  - 时间范围
  - 统计维度（日/周/月）
  - 筛选条件
- 接口同步频率配置：
  - 接口名称
  - 同步方式（实时/定时/手动）
  - 同步间隔/时间点
  - 状态（启用/禁用）
- 接口适配配置：
  - 接口名称、编码
  - 接口地址
  - 请求方式（GET/POST/PUT/DELETE）
  - 请求参数配置
  - 响应格式与字段映射
  - 适配规则/转换逻辑
- 接口日志查询条件：
  - 接口名称
  - 时间范围
  - 调用方
  - 状态（成功/失败）
  - 关键词
- 接口日志分类配置：
  - 分类名称、编码
  - 分类维度
  - 分类规则
- 接口权限认证配置：
  - 认证方式（API Key/Token/OAuth2.0/数字签名）
  - 认证信息（密钥/令牌/证书等）
  - 有效期
  - 权限范围
  - 调用频率限制
- 导出格式选择。


### 输出

- 统计信息列表/图表（各类型统计数据）。
- 统计导出文件。
- 接口对接情况分析报告（成功率、失败率、调用量趋势）。
- 接口同步频率配置列表。
- 接口适配配置列表及详情。
- 接口日志列表及详情（分页展示）。
- 接口日志分类列表。
- 接口权限认证配置列表。
- 各类导出文件。
- 接口告警信息。
- 操作日志记录。


### 异常处理

- 无权限：提示"暂无权限，请联系管理员"。
- 接口调用失败：记录失败日志，触发告警，通知运维人员。
- 接口超时：自动重试（可配置重试次数和间隔），重试失败则告警。
- 认证失败：拒绝调用，记录日志，提示认证信息错误。
- 配置保存失败：高亮错误字段并提示具体原因。
- 导出失败：提示失败原因，支持重试。
- 系统异常：记录日志并提示"操作失败，请联系管理员"。


### 验收标准

- 支持 5 类统计信息的查询与导出（网络电话、短信强提醒、视频会议、视频电话、聊天窗口）。
- 支持接口对接情况分析查询与导出。
- 支持接口同步频率配置的新增、修改、删除、查询、导出。
- 支持接口适配信息的新增、修改、删除、查询、导出。
- 支持接口日志信息的查询、修改、删除、导出。
- 支持接口日志分类的新增、修改、删除、查询、导出。
- 支持接口权限认证配置的新增、修改、删除、查询、导出。
- 支持多种认证方式（API Key、Token、OAuth2.0、数字签名）。
- 接口异常时自动告警。
- 支持接口调用超时自动重试。
- 所有操作记录操作日志。


### 原型元素映射


| 原型元素 | 需求条目 |
| --- | --- |
| 左侧菜单"系统对接" | 模块入口 |
| 统计信息 Tab | 5 类统计信息分类查询 |
| 统计图表/列表 | 统计数据展示与导出 |
| 接口对接分析面板 | 对接情况分析展示 |
| 同步频率配置列表 | 同步频率配置管理 |
| "新增/编辑同步频率"弹窗 | 同步方式、间隔、时间点配置 |
| 接口适配列表 | 接口适配配置管理 |
| "新增/编辑接口适配"弹窗 | 接口地址、参数、响应、适配规则配置 |
| 接口日志列表 | 接口调用日志查询与展示 |
| 日志分类管理面板 | 接口日志分类维护 |
| 权限认证配置列表 | 接口权限认证配置管理 |
| "新增/编辑认证配置"弹窗 | 认证方式、认证信息、有效期、权限范围配置 |
| 告警提示 | 接口异常告警展示 |
| "导出"按钮 | 各类数据导出 |